{# TRACE — faithful single-colour trace of the Anyo at Disenyo figure-eight
   "a+d" loop monogram from source/images/og-image.jpg. This is NOT the
   original bitmap (which carries baked-in text and cannot rotate/tile
   cleanly); the continuous interlocking loop geometry is reproduced here as
   one stroke (currentColor) purely for decorative pattern use. The real
   og-image.jpg bitmap is still placed in the header, hero and footer. #}
<svg viewBox="0 0 260 120" xmlns="http://www.w3.org/2000/svg" aria-hidden="true" focusable="false">
  <g fill="none" stroke="currentColor" stroke-width="22" stroke-linejoin="round">
    <rect x="13"  y="20" width="132" height="80" rx="40"/>
    <rect x="115" y="20" width="132" height="80" rx="40"/>
  </g>
</svg>
